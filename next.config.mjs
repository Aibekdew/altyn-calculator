// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ["ru", "en", "kg"],
    defaultLocale: "ru",
  },
  // …другие настройки
};

export default nextConfig;
